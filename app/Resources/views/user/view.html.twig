{% extends 'template.html.twig' %}

{% block welcometitle %}
    <h1>{{ user.firstName }}</h1>
{% endblock %}

{% block containerbody %}
    <div style="display: inline-block;">
        <a href="{{ path('app') }}">Home</a>
        <a href="{{ path('user_create') }}">Create</a>
        <h2>First name</h2>
        <hr align="left" width="200" color="Grey">
        <a href="#"> {{ user.firstName }}  </a>
    </div>
    <div style=" display: inline-block;">
        <h2>Last name</h2>
        <hr align="left" width="200" color="Grey">
        {{ user.lastName }}

    </div>
    <div style=" display: inline-block;">
        <hr align="left" width="100" color="Grey">
        <a href="{{ path('user_update', {'id': user.id}) }}">Edit</a>
    </div>
    <div style=" display: inline-block;">
        <hr align="left" width="100" color="Grey">
        <a href="{{ path('user_delete', {'id': user.id}) }}">Delete</a>
    </div>
{% endblock %}
